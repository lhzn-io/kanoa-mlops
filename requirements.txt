# kanoa-mlops user requirements
# For data scientists running the example notebooks
#
# LOCAL DEVELOPMENT:
#   cp requirements-local.txt.template requirements-local.txt
#   pip install -r requirements-local.txt
#
# This uses editable local kanoa instead of the git URL below.

# kanoa library (PyPI)
kanoa[gemini,notebook]>=0.4.0

requests>=2.28.0
jupyter>=1.0.0
ipywidgets>=8.0.0
matplotlib>=3.7.0
pandas>=2.0.0
numpy>=1.24.0

# GPU support
torch>=2.0.0  # PyTorch with CUDA support (auto-detects CUDA)
nvidia-ml-py3>=7.352.0  # Python bindings for NVIDIA Management Library
gputil>=1.4.0  # GPU monitoring utilities
vllm>=0.6.3  # Local LLM serving (OpenAI compatible)
